apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "giantmuseumweb.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "giantmuseumweb.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "giantmuseumweb.name" . }}
    spec:
      containers:
        - name: giantmuseumweb
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 3000